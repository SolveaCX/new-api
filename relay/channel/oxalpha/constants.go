package oxalpha

// ModelList contains the provider's documented default model identifier.
// Channel model mappings remain configurable; the adapter does not replace the
// model supplied by a client request.
var ModelList = []string{"ox-alpha"}

var ChannelName = "oxalpha"
